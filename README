Windows Registry in Node.Js using ffi with GoLang c-shared DLL.<br />
This was to demo that you can use GoLang c-shared DLL (Go>=1.10) with ffi.<br />
Uploading to npm for ease of use 😅.<br />

Example
=======

```js

const regedit = require("regodit");

(async()=>{
try{

  //promise
  let res = await regedit.promises.RegListAllSubkeys("HKCU","Software/Valve");
  console.log(res);
  
  //sync
  let res2 = regedit.RegListAllSubkeys("HKCU","Software/Valve");
  console.log(res2);


}catch(err){ throw err }
})().catch(console.error);

```

API
===

`RegKeyExists`<br />
(root, key ) bool

`RegListAllSubkeys`<br />
(root , key ) array

`RegListAllValues`<br />
(root , key ) array

`RegQueryValue` // REG_SZ & REG_EXPAND_SZ<br />
(root , key , name ) string

`RegQueryValueAndExpand` // REG_EXPAND_SZ (expands environment-variable s)<br />
(root , key , name ) string

`RegQueryBinaryValue` //REG_BINARY<br />
(root , key , name ) string

`RegQueryIntegerValue` //REG_DWORD & REG_QWORD<br />
(root , key , name ) string

`RegWriteValue`<br />
(root , key , name , value )

`RegWriteExpandValue`<br />
(root , key , name , value )

`RegWriteBinaryValue`<br />
(root , key , name , value )

`RegWriteDwordValue`<br />
(root , key , name , value )

`RegWriteQwordValue`<br />
(root , key , name , value ) 

`RegDeleteKeyValue`<br />
(root , key , name )
